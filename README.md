To test the reliable UDP transfer, you can generate an input file of any size using:

`yes "This is a test file for reliable UDP transfer." | head -c 500000 > input.txt`

This example creates a 500000 byte text file, but youcan use it create a file of your 
chosen size by simply adjusting the integer